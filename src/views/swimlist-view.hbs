{{> menu}}

<section class="section">
<div class="box">
  <div class="media">
    <div class="media-content">
      <h2 class="title is-20">{{swimlist.title}}</h2>
    </div>
    <div class="column">
      {{> swimlist-image }}
    </div>
  </div>
</div>
</section>

<section>
  {{> list-spot}}
  {{> add-spot}}
</section>

